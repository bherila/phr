# pnpm 11 reads `allowBuilds`; `onlyBuiltDependencies` is retained because older
# pnpm versions ignore `allowBuilds`. Both list the same packages deliberately.
allowBuilds:
  '@swc/core': true
  unrs-resolver: true
onlyBuiltDependencies:
  - '@swc/core'
  - unrs-resolver
