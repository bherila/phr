<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="AGENT_API_MUTATION_DIGEST_KEY" value="synthetic-agent-mutation-digest-key-not-for-production" force="true"/>
        <!-- The suite reaches ~121 MB of resident allocations by the time it gets to the
             PHR import tests, and mime_content_type() then needs a further ~7 MB to load
             libmagic's magic database — enough to blow a 128 MB limit on a 19-byte fixture.
             Nothing is leaking; the limit is simply too low for the whole suite in one
             process.

             This has to live here rather than in the invocation, because the limit
             otherwise depends on how the suite is started. CI runs
             `php -d memory_limit=1G vendor/bin/phpunit`, which is fine, but
             `php artisan test` spawns phpunit as a subprocess that does NOT inherit -d,
             so it silently falls back to php.ini (128 M on a stock macOS build) and dies
             with "Premature end of PHP process". Setting it here covers every entry point:
             artisan test, phpunit directly, and IDE runners. -->
        <ini name="memory_limit" value="1G"/>
        <env name="APP_ENV" value="testing"/>
        <!-- Pinned so the suite does not inherit a developer's local APP_URL. Several tests
             assert generated absolute URLs (signed DICOM instance URLs, volume-cache manifests),
             and a local .env pointing at e.g. http://localhost:8000 fails them for reasons that
             have nothing to do with the code under test. -->
        <env name="APP_URL" value="http://localhost"/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
